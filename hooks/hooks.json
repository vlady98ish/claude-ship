{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"additionalContext\": \"MANDATORY RULE: Have you invoked /ship-router before editing code? If not — STOP immediately and call the ship-router skill first. Every development task MUST be routed. See CLAUDE.md.\"}'"
          }
        ]
      }
    ]
  }
}
